#include <iostream>

using namespace std;

int main(){

    // 1
    cout << "Enter two numbers" << endl;

    // 2
    int x = 1, y = 1, z = 1, p;

    p = x + y + z;

    // 3
    int m = 2, v = 2;

    if ((m + m) > v) cout << "Yes" << endl;

    // 4
    int s, r, t;

    cin >> s >> r >> t;
}