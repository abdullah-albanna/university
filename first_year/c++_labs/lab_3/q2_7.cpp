// Write equivalent compound statements if possible

int main() {

    int x, y, sum, num, z;

    /*
    x = 2 * x
    x = x + y -2;
    sum = sum + num

    z = z * x + 2 * z;
    y = y / (x + 5)
    */

    x *= 2;
    x += y - 2;
    sum += num;
    z *= x + 2 * z;
    y /= (x + 5);

    
}